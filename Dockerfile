# Railway builds this file. It deploys the latest stable Outline release exactly
# as published by upstream, so the Node.js runtime always matches the bundled
# node_modules. The source code in this repository is not built.
FROM outlinewiki/outline:latest
